RUNS_DIR="var/runs"
STATE_DIR="var/state"
LOCK_DIR="var/state/locks"

SSH_CONNECT_TIMEOUT_SECS=10
SSH_COMMAND_TIMEOUT_SECS=30
MAX_PARALLEL_HOSTS=5

LOG_WINDOW="24h"
COLLECT_AUTH_LOGS=false

AI_ENABLED=false
AI_MODEL=""
AI_MAX_INPUT_BYTES=200000
AI_REDACT_PATTERNS=""

EMAIL_ENABLED=true
EMAIL_TO=""
EMAIL_FROM=""
EMAIL_SUBJECT_PREFIX="[watchtower]"

RETENTION_DAYS_REPORTS=30
RETENTION_DAYS_ARTIFACTS=7
