# serverWatchTower Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-01-30

## Active Technologies
- Bash (POSIX-ish + common GNU userland) + cron, ssh, scp (optional), awk/sed/grep, coreutils, gzip/tar, mailx (or sendmail-compatible), curl, jq (optional but recommended) (001-server-watchtower)
- Filesystem (per-run artifacts + reports + a small state file for diffing “new warnings”) (001-server-watchtower)

- (001-server-watchtower)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for 

## Code Style

: Follow standard conventions

## Recent Changes
- 001-server-watchtower: Added Bash (POSIX-ish + common GNU userland) + cron, ssh, scp (optional), awk/sed/grep, coreutils, gzip/tar, mailx (or sendmail-compatible), curl, jq (optional but recommended)

- 001-server-watchtower: Added

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
